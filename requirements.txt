# Telegram Bot
python-telegram-bot[job-queue]>=21.0

# Google APIs
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Google Gemini AI
google-genai>=1.0.0

# Web server (webhook mode)
aiohttp>=3.9.0

# Database
aiosqlite>=0.19.0

# Environment
python-dotenv>=1.0.0

# Utilities
pytz>=2024.1

# Calendar invite parsing
icalendar>=6.0.0

# Scheduler (optional — only needed for local polling mode)
apscheduler>=3.10.0
